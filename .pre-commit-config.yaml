repos:


  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.4.1
    hooks:

      - id: ruff-format
        args:
          - '--config=pyproject.toml'
          - '--check'
      - id: ruff
        args:
          - '--config=pyproject.toml'